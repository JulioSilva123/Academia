<?xml version="1.0" encoding="UTF-8"?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:jft.ToDoXamarinForm.Views"
            Title="jft.ToDoXamarinForm"
             x:Class="jft.ToDoXamarinForm.AppShell">
   
    
    
    <Shell.Resources>
        <ResourceDictionary>
            <Style x:Key="BaseStyle" TargetType="Element">
                <Setter Property="Shell.BackgroundColor" Value="{StaticResource Primary}" />
                <Setter Property="Shell.ForegroundColor" Value="White" />
                <Setter Property="Shell.TitleColor" Value="White" />
                <Setter Property="Shell.DisabledColor" Value="#B4FFFFFF" />
                <Setter Property="Shell.UnselectedColor" Value="#95FFFFFF" />
                <Setter Property="Shell.TabBarBackgroundColor" Value="{StaticResource Primary}" />
                <Setter Property="Shell.TabBarForegroundColor" Value="White"/>
                <Setter Property="Shell.TabBarUnselectedColor" Value="#95FFFFFF"/>
                <Setter Property="Shell.TabBarTitleColor" Value="White"/>
            </Style>
            <Style TargetType="TabBar" BasedOn="{StaticResource BaseStyle}" />
            <Style TargetType="FlyoutItem" BasedOn="{StaticResource BaseStyle}" />
        </ResourceDictionary>
    </Shell.Resources>

    <TabBar>
        <ShellContent Title="Grupos"  Icon="icon.png"  Route="GruposAtividadesListPage" ContentTemplate="{DataTemplate local:GruposAtividadesListPage}"  />
        <ShellContent Title="Tipos" Icon="icon.png"   Route="TiposAtividadesListPage"  ContentTemplate="{DataTemplate local:TiposAtividadesListPage}"  />
        <ShellContent Title="Atividades" Icon="icon.png"  Route="AtividadesListPage"  ContentTemplate="{DataTemplate local:AtividadesListPage}"  />        
        <ShellContent Title="Diário" Icon="icon.png"   Route="Itens_AtividadesListPage"  ContentTemplate="{DataTemplate local:Itens_AtividadesListPage}"  />
    </TabBar>

    <!--
        If you would like to navigate to this content you can do so by calling
        await Shell.Current.GoToAsync("//LoginPage");
    -->
    <TabBar>
        <ShellContent Title="Login"   />
    </TabBar>
    
    
    
</Shell>