<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Title="{Binding Title}"
             xmlns:local="clr-namespace:jft.ToDoXamarinForm.ViewModels"
             xmlns:model="clr-namespace:jft.ToDoXamarinForm.Models"
             x:Class="jft.ToDoXamarinForm.Views.AtividadesListPage">


    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Add" Clicked="OnItemAdded">
            <!--<ToolbarItem.IconImageSource>
                <OnPlatform x:TypeArguments="ImageSource">
                    <On Platform="Android, UWP"
                        Value="plus.png" />
                </OnPlatform>
            </ToolbarItem.IconImageSource>-->
        </ToolbarItem>
        <!--<ToolbarItem Text="+" Clicked="OnGrupoItemAdded">
            <ToolbarItem.IconImageSource>
                <OnPlatform x:TypeArguments="ImageSource">
                    <On Platform="Android, UWP"
                        Value="plus.png" />
                </OnPlatform>
            </ToolbarItem.IconImageSource>
        </ToolbarItem>-->
    </ContentPage.ToolbarItems>
    
    
    
    
    
    
    <StackLayout  >

        <RefreshView x:DataType="local:AtividadesListPageViewModel" 
                     Command="{Binding LoadItemsCommand}" 
                     IsRefreshing="{Binding IsBusy, Mode=TwoWay}">


            <!--<Grid ColumnDefinitions="*,*,*">
                <Label Text="Column 1" FontAttributes="Bold" />
                <Label Text="Column 2" FontAttributes="Bold" />
            </Grid>-->

             





            <CollectionView 
                x:Name="ItemsListView"
                BackgroundColor="#F9F9F9"  
                EmptyView="Nenhum item encontrado"  
                ItemsSource="{Binding Items}"
                SelectionMode="None">
                
                <!--<CollectionView.ItemsLayout>
                    <GridItemsLayout Orientation="Vertical" Span="2"/>
                </CollectionView.ItemsLayout>-->
                
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout Padding="10" x:DataType="model:AtividadesView">

                            <Frame BorderColor="LightGray" CornerRadius="15" 
                           HasShadow="True" Padding="5">
                                
                                
                                 <Grid Padding="5" ColumnSpacing="0" RowSpacing="0" 
                              Margin="2">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="auto"/>
                                        <!--<RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>-->
                                    </Grid.RowDefinitions>
                                     
                                     
                                    <!--<Image  Grid.Row="0"   
                                   Source="favorito.png"  
                                   Aspect="AspectFit"   
                                   HorizontalOptions="End" >
                                    </Image>
                                    <Image Grid.Row="1" Source="{Binding ImagemUrl}" Aspect="Fill"/>
                                    <Label Grid.Row="2" Text="{Binding Titulo}" TextColor="#14B7F8"
                                HorizontalTextAlignment="Center" VerticalOptions="CenterAndExpand"/>
                                    <Label Grid.Row="3" Text="{Binding Preco, StringFormat='${0:N}'}"
                              HorizontalTextAlignment="Center" VerticalOptions="CenterAndExpand" 
                                   FontAttributes="Bold"/>-->

                                    <Label  Grid.Row="0" Text="{Binding nm_atividade}" 
                            LineBreakMode="NoWrap" 
                                   TextColor="Black"
                            Style="{DynamicResource ListItemTextStyle}" 
                            FontSize="16" />

                                    <Label  Grid.Row="1" Text="{Binding te_descricao}" 
                            LineBreakMode="WordWrap" TextColor="Black"
                            Style="{DynamicResource ListItemDetailTextStyle}"
                            FontSize="13" />
                                
                                
                                </Grid> 
                            

                            
                            </Frame>
                            
                            
                            <StackLayout.GestureRecognizers>
                                <TapGestureRecognizer 
                                NumberOfTapsRequired="1"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type local:AtividadesListPageViewModel}}, Path=ItemTapped}"		
                                CommandParameter="{Binding .}">
                                </TapGestureRecognizer>
                            </StackLayout.GestureRecognizers>
                                
                        </StackLayout>
                            
                    </DataTemplate>
                </CollectionView.ItemTemplate>
                
            </CollectionView>
        </RefreshView>


       
    </StackLayout>
</ContentPage>